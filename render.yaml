services:
  - type: web
    name: stockgpt-for-siva
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --worker-class gthread --threads 4 --timeout 180
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
